#!/usr/bin/perl
#------------------------------------------------------------------
# a shell around chartMaker.pm
#------------------------------------------------------------------
# for separate Cava packaging of a GUI executable
# which needs a separate script.

use strict;
use warnings;

require "chartMaker.pm";
	# run the program

1;
